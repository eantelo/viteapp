/**
 * Standard layout class for all protected pages.
 * Applied as className on DashboardLayout.
 *
 * Responsive: p-3 (mobile) → p-4 (tablet) → p-6 (desktop)
 * Gap-4 provides comfortable breathing room between sections.
 */
export const PAGE_LAYOUT_CLASS = "flex flex-1 flex-col gap-4 p-3 md:p-4 lg:p-6";
